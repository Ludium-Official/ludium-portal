query programsV2($pagination: PaginationInput) {
  programsV2(pagination: $pagination) {
    data {
      id
      title
      description
      skills
      deadline
      invitedMembers
      status
      visibility
      networkId
      price
      token_id
      createdAt
      updatedAt
    }
    count
  }
}
