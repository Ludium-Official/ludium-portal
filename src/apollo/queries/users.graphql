query users($input: PaginationInput) {
  users(pagination: $input) {
    count
    data {
      about
      avatar
      email
      firstName
      id
      image
      lastName
      links {
        title
        url
      }
      organizationName
    }
  }
}
