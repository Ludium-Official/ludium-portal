query user($id: ID!) {
  user(id: $id) {
    summary
    about
    avatar
    email
    firstName
    id
    image
    lastName
    links {
      title
      url
    }
    organizationName
    walletAddress
  }
}
