query profile {
  profile {
    about
    summary
    email
    firstName
    id
    # isAdmin
    role
    image
    lastName
    links {
      title
      url
    }
    organizationName
    walletAddress
  }
}
