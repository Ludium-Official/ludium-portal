query hackathonPrizes($hackathonId: ID!) {
  hackathonPrizes(hackathonId: $hackathonId) {
    id
    hackathonId
    name
    about
    prize
    sponsorImage
    link
    createdAt
    updatedAt
  }
}
