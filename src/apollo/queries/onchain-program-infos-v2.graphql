query onchainProgramInfosV2($pagination: PaginationInput) {
  onchainProgramInfosV2(pagination: $pagination) {
    data {
      id
      networkId
      smartContractId
      onchainProgramId
      status
      createdAt
      tx
    }
    count
  }
}
