query post($id: ID!) {
  post(id: $id) {
    id
    title
    content
    image
    keywords {
      id
      name
    }
    author {
      id
      firstName
      lastName
      image
    }
    createdAt
  }
}
