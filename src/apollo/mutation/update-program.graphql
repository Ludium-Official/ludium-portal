mutation updateProgram($input: UpdateProgramInput!) {
  updateProgram(input: $input) {
    currency
    deadline
    description
    id
    name
    price
    status
    summary

    validators {
      about
      email
      firstName
      id
      loginType
      organizationName
      role
      summary
      walletAddress
    }
  }
}
