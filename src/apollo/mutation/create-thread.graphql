mutation createThread($input: CreateThreadInput!) {
  createThread(input: $input) {
    id
    content
    images
    authorNickname
    authorProfileImage
    likeCount
    dislikeCount
    replyCount
    isLiked
    isDisliked
    createdAt
    updatedAt
  }
}
