mutation updateArticleComment($input: UpdateArticleCommentInput!) {
  updateArticleComment(input: $input) {
    id
    articleId
    authorId
    content
    parentId
    likeCount
    dislikeCount
    isLiked
    isDisliked
    createdAt
    updatedAt
    deletedAt
  }
}
