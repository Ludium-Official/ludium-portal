mutation reclaimProgram($programId: ID!, $txHash: String) {
  reclaimProgram(programId: $programId, txHash: $txHash) {
    id
    name
    reclaimed
    reclaimTxHash
    reclaimedAt
    canReclaim
  }
}
