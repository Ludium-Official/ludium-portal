mutation submitMilestone($input: SubmitMilestoneInput!) {
  submitMilestone(input: $input) {
    currency
    description
    id
    links {
      title
      url
    }
    price
    status
    title
  }
}
