mutation updateProgramV2($id: ID!, $input: UpdateProgramV2Input!) {
  updateProgramV2(id: $id, input: $input) {
    title
    description
    skills
    deadline
    invitedMembers
    status
    visibility
    networkId
    price
    token_id
  }
}
