mutation updateProfile($input: UserUpdateInput!) {
  updateProfile(input: $input) {
    about
    summary
    email
    firstName
    id
    image
    lastName
    links {
      title
      url
    }
    organizationName
  }
}
