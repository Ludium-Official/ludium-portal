mutation createProgram($input: CreateProgramInput!) {
  createProgram(input: $input) {
    currency
    deadline
    description
    id
    links {
      title
      url
    }
    name
    price
    status
    summary
  }
}
