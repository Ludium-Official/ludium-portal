mutation login(
  $email: String!
  $userId: ID!
  $walletId: String
  $network: String
  $address: String
) {
  login(email: $email, userId: $userId, walletId: $walletId, network: $network, address: $address)
  # token
  # userRoles
}
