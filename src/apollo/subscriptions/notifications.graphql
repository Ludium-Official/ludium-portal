subscription notifications($input: PaginationInput) {
  notifications(pagination: $input) {
    count
    data {
      id
      title
      content
      type
      action
      entityId
      metadata
      readAt
      createdAt
    }
  }
}
